package Repository;

import Entity.MetronomeSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MetronomeSettingsRepository extends JpaRepository<MetronomeSettings, Long> {

}
